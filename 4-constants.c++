/*
There may be a situation where we don't want the value of a variable to be changed that's where we use constants.
*/

#include<iostream>

int main() {
    const double PI = 3.14;
    // We can't assign different value to this PI
    return 0;
}